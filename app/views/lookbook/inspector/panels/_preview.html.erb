<%= lb_inspector_panel(id: dom_id(target, :preview_panel), key: (target.object_id.to_s + request.query_parameters.to_json)) do %>
  <%= lb_viewport(target.preview_path(request.query_parameters), id: "preview-viewport") %>
<% end %>
