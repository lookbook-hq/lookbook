<%= lookbook_inspector_panel(id: dom_id(@target, :preview_panel), key: @target.source_checksum) do %>
  <%= lookbook_viewport(@target.preview_path(request.query_parameters), id: "preview-viewport") %>
<% end %>
