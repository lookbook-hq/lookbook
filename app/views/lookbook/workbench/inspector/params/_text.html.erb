<input
  id="param-<%= param[:name] %>"
  class="form-input"
  type="<%= param[:input_type] %>"
  name="<%= param[:name] %>"
  value="<%= params[param[:name]] || param[:default] %>"
  @keyup.stop.debounce.200="update($el.name, $el.value)"
  data-param-input>