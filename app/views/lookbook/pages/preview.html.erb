<%=
  lb_pane(id: dom_id(@preview)) do |pane|
    pane.with_toolbar do |toolbar|
      toolbar.with_breadcrumb(@config.previews_nav_label, @preview.ancestors, @preview)      
    end
    
    lb_page(@page) do |page|
      @page_content
    end
  end
%>