<%= render_component :button,
  **@button_attrs,
  x_data: alpine_data,
  "@click.prevent": "copyToClipboard" do |button| %>
  <% button.icon @icon, size: icon_size, x_show: "!done", cloak: true %>
  <%= render_component :icon, :check, size: icon_size, class: "text-green-500", x_show: "done", cloak: true  %>
  <% if content %>
    <div x-ref="copyContent" class="hidden"><%== content %></div>
  <% end %>
<% end %>