<%= render component_tag(:sl_tree_item,
  href: inspect_path,
  key: lookup_path,
  x: {"data": "navTreeItem"}
) do %>
  <sl-icon name="<%= icon %>"></sl-icon>

  <%= label %>
  
  <% children&.each do |node| %>
    <%= lb_nav_tree_item(node) %>
  <% end %>
<% end %>
