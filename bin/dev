#!/usr/bin/env bash

bundle install
npm install

if ! foreman version &> /dev/null
then
  echo "Installing foreman..."
  gem install foreman
fi

npm run clean
npm run copy
foreman start -f Procfile.dev "$@"

