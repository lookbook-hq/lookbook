<%= tag.div **html_attrs, class: [html_attrs[:class], "not-prose"] do %>
  <table>
    <tbody>
      <% params.each do |param| %>
        <% unless options_only %>
          <tr>
            <td class="w-[20%]">
              <code class="text-indigo-600"><%= param[:name] %></code>
            </td>
            <td class="w-[10%]">
              <code class="text-gray-500"><%= param[:types] %></code>
            </td>
            <td>
              <%= markdown param[:text] %>
            </td>
          </tr>
        <% end %>
        <% if param[:opts].present? %>
          <% param[:opts].each do |opt| %>
            <tr>
              <td class="w-[20%]">
                <code class="flex items-center">
                  <span class="text-indigo-500 opacity-60"><%= param[:name] %>[</span>
                  <span class="text-indigo-600">:<%= opt[:name] %></span>
                  <span class="text-indigo-500 opacity-60">]</span>
                </code>
              </td>
              <td class="w-[10%]">
                <code class="text-gray-500"><%= opt[:types] %></code>
              </td>
              <td>
                <%= markdown opt[:text] %>
              </td>
            </tr>
          <% end %>
        <% end %>
      <% end %>
    </tbody>
  </table>
<% end %>